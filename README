#README

# Call SplashScree.jar to run the application

# If necessary call RebuildDB.jar to rebuild the database

# If you exit the secretary menu you need to relaunch the app to access the partner menus